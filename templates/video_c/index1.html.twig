{% extends 'base.html.twig' %}

{% block title %}Video Category{% endblock %}

{% block body %}

          {#<div class="row">#}
      {#{% for video in videosc %}#}
            {#{% if element == video.categoryv.id  %}#}
                  {#<div class="col-lg-4 col-md-4 col-xs-6 thumb" id="{{ video.id }}">#}
                      {#<iframe class="embed-responsive-item" src="{{ vich_uploader_asset(video, 'videoFile') }}" allowfullscreen></iframe>#}
                  {#</div>#}
            {#{% endif %}#}
             {#{% else %}#}
                  {#Test Ok#}
      {#{% endfor %}#}
          {#</div>#}

    <br>
    <div class="container">

        <div class="row">
            {% for video in videosc %}
                {% if element == video.categoryv.id  %}

                <div class="col-lg-4 col-md-12 mb-4">

                    {% set foo = video.name %}
                    <br>
                    <div class="modal fade" id="{{ foo }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
                        <div class="modal-dialog modal-lg" role="document">


                            <div class="modal-content">


                                <div class="modal-body mb-0 p-0">

                                    <div class="embed-responsive embed-responsive-16by9 z-depth-1-half">
                                        {#<iframe class="embed-responsive-item" src="{{ vich_uploader_asset(video, 'videoFile') }}"#}
                                                {#allowfullscreen></iframe>#}
                                        <video  controls id="video13">
                                            <source src="{{ vich_uploader_asset(video, 'videoFile') }}" type="video/mp4">
                                            <source src="{{ vich_uploader_asset(video, 'videoFile') }}" type="video/ogg">
                                            Your browser does not support HTML5 video.
                                        </video>
                                        <br>

                                    </div>

                                </div>


                                <div class="modal-footer justify-content-center">
                                    <span class="mr-4">Anesaroudine France</span>
                                    <button type="button" class="btn btn-outline-primary btn-rounded btn-md ml-4" id="test1" data-dismiss="modal">Fermer</button>

                                </div>

                            </div>


                        </div>
                    </div>


                    {#<a><img class="img-fluid z-depth-1" src="https://mdbootstrap.com/img/screens/yt/screen-video-1.jpg" alt="video"#}
                    {#data-toggle="modal" data-target="#{{ foo }}"></a>#}
                    {#<iframe class="embed-responsive-item" src="{{ vich_uploader_asset(video, 'videoFile') }}"#}
                    {#allowfullscreen></iframe>#}

                    <div class="card" style="width: 18rem;">
                        <video class="embed-responsive-item" id="video1" src="{{ vich_uploader_asset(video, 'videoFile') }}"
                                allowfullscreen  controls preload="none" type="video/mp4"></video>
                        <div class="card-body">
                            <h5 class="card-title">{{ foo }}</h5>

                            <button class="btn btn-success btn-lg btn-block" id="testok" alt="video"
                               data-toggle="modal" data-target="#{{ foo }}">Regarder la</button>
                        </div>
                    </div>
                    <br>

                    <br>

                </div>
                {% endif %}
            {% endfor %}
        </div>

    </div>

    <br>
{% endblock %}
{% block javascripts %}
        <script>

    </script>
{% endblock %}
