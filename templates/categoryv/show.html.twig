{% extends 'base.html.twig' %}

{% block title %}Categoryv{% endblock %}

{% block body %}
    <div class="container">
    <h1>Categoryv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoryv.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categoryv.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoryv_index') }}">back to list</a>

    <a href="{{ path('categoryv_edit', {'id': categoryv.id}) }}">edit</a>

    {{ include('categoryv/_delete_form.html.twig') }}
    </div>
{% endblock %}
